{% extends 'base.html.twig' %}

{% block body %}
	<h1 class="text-center">Register</h1>
	{% if notification %}
		<div class="alert alert-info">{{notification}}</div>
		<hr>
	{% endif %}
	{{form(form)}}{% endblock %}
